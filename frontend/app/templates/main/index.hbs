{{page-title "Index"}}

<container class="row">
    {{#each this.model.people as |person|}}
    {{#if person.is_active}}
    <PersonCardConsumable @person={{person}} class="col-sm-6" style="padding: 20px;" />
    {{/if}}
    {{/each}}
</container>