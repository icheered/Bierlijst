<button type="button" {{on "click" (fn this.openDialog)}}>Add new person</button>

<MdcDialog @title="Add a new person" @negativeButton={{hash action="close" label="Cancel" }} @positiveButton={{hash
    action="save" label="Save" closing=this.save}} @open={{this.open}}>
    <form {{action 'save' on='submit' }}>
        <div class="form-floating mb-3">
            <div style="padding: 10px;">
                <label>Person name</label>
                <input class="form-control" type="text" placeholer="Name" onchange={{action updateName }}>
            </div>

        </div>
    </form>


</MdcDialog>